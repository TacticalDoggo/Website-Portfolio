# Build Tracker

Updated by Claude Code after commits. Manual updates welcome.
Last updated: 2026-04-29 (step 13)

---

## Build Order Status

| # | Task | Status | Notes |
|---|------|--------|-------|
| 1 | Project scaffold | DONE | Next.js 16.2.4 + React 19 + Tailwind v4 + Fraunces/Inter/JetBrains Mono via next/font. Foundational tokens only (bg-page, text-primary). Throwaway placeholder at app/page.tsx (replaced in step 4). |
| 2 | Design tokens | DONE | Full SITE_SPEC.md sections 1.2 + 1.3 expressed. Raw CSS variables in :root match spec names verbatim; @theme inline mirrors them with category prefixes preserved (utilities like bg-bg-page, text-text-primary, border-border-hairline). Element defaults for h1/h2/h3/body/small with desktop breakpoint at 768px. @utility for pullquote, mono-label, mono-body. Sitewide focus-visible outline (uses --accent) and prefers-reduced-motion baseline added. Placeholder app/page.tsx refactored to consume tokens; no inline styles or arbitrary values remain. |
| 3 | Masthead and global footer | DONE | Pure components, not yet wired into pages (step 4 imports them). Masthead is a Client Component (sticky positioning, scroll-driven hairline border at >=40px, mobile hamburger overlay, active-link via usePathname). Wordmark composes spans (mono-label + serif), never <h1>. Footer is a Server Component with mode prop ('contact' | 'sitewide'); outer footer has id="contact" in BOTH modes per spec lock. Inverted-section overrides #A8A6A0 (mono labels on dark) and #444341 (dividers) are scoped to Footer.tsx with comments. Identity references centralized in app/_data/identity.ts. |
| 4 | Homepage | DONE | Hero, selected work cards, Now block, contact footer all live at `/`. ProjectCard, RevealOnScroll, SiteFooter components landed alongside. Real photos at `/public/projects/[slug]/` deferred per the standing pre-launch deliverable; cards render typographic placeholders (88px Fraunces numerals on `bg-alt`) per section 1.10.5. SEO `metadata` export covers title, description, canonical, and OG tags; JSON-LD WebSite schema deferred to step 15 per CLAUDE.md build order. |
| 5 | Project card component + projects.ts | DONE | Landed early as part of step 4 (`app/_components/ProjectCard.tsx` + `app/_data/projects.ts`). Same component will render on /projects index in step 6. Source casing is natural; `mono-label` utility renders the visual ALL-CAPS. |
| 6 | /projects index | DONE | Header (eyebrow + H1 + dek) + 5-card stack in locked quality-weighted order at `/projects`. ProjectCard reused verbatim; data restructured so `homepageProjects` is now `allProjects.slice(0, 3)`. Nicular card is the first site-wide use of the § 1.10.5 typographic placeholder (bg-bg-paper + caption); cards 1/2/3/5 keep the deferred-photo placeholder until photo-wiring lands. JSON-LD `CollectionPage` + `ItemList` shipped inline (deviation: pulled forward from step 15). SEO `metadata` covers title, description, canonical, OG. OG image route still deferred to step 16. |
| 7 | NASA Circadian Lighting | DONE | Six-section default case study pattern (SITE_SPEC § 1.9.2) live at `/projects/nasa-circadian-lighting`. New shared components: Breadcrumb, ProjectHeader, RoleBulletList, ProjectPageFooter, SectionDivider. SectionDivider promoted from inline copies on the homepage and /projects index; both pages now import from the shared module. Page copy verbatim from `docs/pass2-nasa-circadian-lighting.md`. Two protected winks (dek + outcome) preserved per spec § 3.7. Hero photo (`public/projects/nasa-circadian-lighting/demo-rig.jpg`, 4000×2252) served via `next/image` with `priority`; container is `aspect-[16/10]` with `object-cover` (~6% horizontal crop on the 16:9 source, pre-approved). JSON-LD `BreadcrumbList` + `CreativeWork` shipped inline this step (deviation logged). Sitewide CSS change: `scroll-behavior: smooth` on `html`, gated by the existing `prefers-reduced-motion` override. Post-commit Level 3 review findings recorded below. |
| 8 | T&S Machines | DONE | Five-section ongoing-project case study pattern (SITE_SPEC § 1.9.3) live at `/projects/ts-machines`. First in-code use of the variant: `Current state` section (replaces NASA's `Outcome`), and the dedicated `Side-project` section between `The work` and `My role`. All copy verbatim from `docs/pass2-ts-machines.md`; zero winks per locked no-wink genre (SITE_SPEC § 1.12.2). Reused step-7 components verbatim (no new components): ProjectHeader, RoleBulletList, ProjectPageFooter, SectionDivider, RevealOnScroll. Hero photo (`public/projects/ts-machines/machine-in-action.jpg`, 4032×3024, 4:3) wired via `next/image` priority into the existing `aspect-[16/10]` container; ~16.7% combined vertical crop pre-approved during the step-8 browser pass (decision-to-record logged in `deviations.md`). JSON-LD `BreadcrumbList` + `CreativeWork` shipped inline (deviation, same shape as NASA / projects-index). Bundled fix in the same commit: layout-level title template was double-suffixing `- Alex Bacallao` on three pre-existing pages (homepage, /projects, NASA); all four pages now render their spec-required `<title>` exactly. Section eyebrow source is natural casing (sentence case for `§ 01 · Industrial · Software · Since 2019`, hyphen-minus for `§ 02 - The work` etc.); first project-page surface that follows the standing source-casing rule. Post-commit Level 3 review findings recorded below. |
| 9 | HackZurich Migros | DONE | Six-section default case study pattern (SITE_SPEC § 1.9.2) live at `/projects/hackzurich-migros`. All copy verbatim from `docs/pass2-hackzurich-migros.md`; zero winks per locked no-wink genre (SITE_SPEC § 1.12.2). Two new patterns landed: (1) first inline figure on the site - `<figure>` + `<Image>` + `<figcaption>` markup inlined in § 03 between paragraphs 1 and 2 (FIG. 02, 16:10 native to the source 1184×746, 32px top/bottom margin), not extracted to a shared component per the abstraction-threshold rule. (2) First non-16:10 hero - `ProjectHeader` gains an optional `aspectRatio` prop (`'16/10' \| '4/3' \| '1/1'`; default `'16/10'`); HackZurich passes `'1/1'` for the 877×853 team-after-win photo to preserve all 5 team members (vs ~35.7% vertical crop at the 16:10 default that would slice through heads or feet). NASA / T&S unchanged - omit the prop, fall through to 16:10 default; verified post-build via `curl ... \| grep aspect-`. CreativeWork JSON-LD ships the spec-locked `award: '1st place, Migros challenge, HackZurich 2022'` (first project page on the site to ship an award field). Section eyebrow source casing follows the standing rule (`§ 01 · Web · Hackathon · 2022`, `§ 02 - The problem`, etc.). Bare-title pattern matches the step 8 fix; `<title>` renders as `Supply chain rerouting at HackZurich - Alex Bacallao`. Post-commit Level 3 review findings recorded below. |
| 10 | Nicular | DONE | Six-section default case study pattern (SITE_SPEC § 1.9.2) live at `/projects/nicular`. First page on the site to use the typographic placeholder hero (SITE_SPEC § 1.10.5) - exercises `ProjectHeader`'s `mode: 'placeholder'` branch for the first time since it was added speculatively in step 7. All copy verbatim from `docs/pass2-nicular.md`; zero winks per locked no-wink genre (SITE_SPEC § 1.12.2). The mixed-depth disclaimer line in § 03 paragraph 2 (`"Some of that was real configuration work for the integrations I owned; some was just calling the right framework methods correctly."`) ships verbatim per SITE_SPEC § 1.12.4 - that is honest scope framing, not a wink, same reading as T&S § 02 paragraph 5. No "why I left" treatment on the page per SITE_SPEC § 1.12.3; the role's 2-month duration is visible from the type label, dek, JSON-LD `dateCreated: "2022-11"`, and the lede line in § 04 (`"Two months total tenure"`) but is never explained. Reused step-7/8/9 components verbatim (no new components): ProjectHeader, RoleBulletList, ProjectPageFooter, SectionDivider, RevealOnScroll. ProjectHeader change: one-line className edit in the placeholder branch (numeral color `text-text-secondary` -> `text-text-primary`), aligning the speculative branch to spec § 10.1 on first real use. The /projects index Nicular card placeholder (`ProjectCard.tsx`) keeps its existing `text-text-secondary` numeral - intentionally different from the page hero per SITE_SPEC § 1.10.1 (the card pattern doesn't pin numeral color). Hero is `aspect-[16/10]` per spec default; no `aspectRatio` override. JSON-LD `BreadcrumbList` + `CreativeWork` shipped inline (deviation, same shape as NASA / T&S / HackZurich / projects-index). Bare-title pattern matches steps 8-9; `<title>` renders as `Patient messaging at Nicular - Alex Bacallao`. Section eyebrow source casing follows the standing rule (`§ 01 · Healthcare · Full-time · Late 2022`, `§ 02 - The problem`, etc.). Post-commit Level 3 review findings recorded below. |
| 11 | SofaBot | DONE | Four-section compressed hobbyist / club case study pattern (SITE_SPEC § 1.9.4) live at `/projects/sofabot`. First and only use of the compressed pattern on the site - § 02 `The build` (3 paragraphs) absorbs what would be Problem + Approach + My Role on the default 6-section pattern; § 03 `Outcome` (3 paragraphs) closes with the homecoming-parade beat → voltage lesson → current-state cadence. Total ~515 words across two prose sections. All copy verbatim from `docs/pass2-sofabot.md`. **First and only project page on the site with a non-zero wink budget** (SITE_SPEC § 1.12.2 / § 1.6 voice rule 5): two winks earned by the project's natural absurdity, both verbatim, well-separated, unified register - wink #1 in the subtitle (wink-as-noun-phrase: `"a free couch and a stripped-for-parts go-kart"`), wink #2 closing § 03 paragraph 1 (wink-by-understatement: `"The sofa drove, in a parade."`). The `"SofaBot at rest"` figure caption is voice, not a third wink. The `"We never got there."` fragment in § 02 paragraph 3 ships verbatim per spec § 11.5 - illustrative fragment, not a wink. The `Then COVID hit in 2020, and the project went on hard pause` close-of-§02 / `The project went on hard pause for COVID in 2020 and has been on slow-burner status since 2022` open-of-§03 bookend ships verbatim - deliberate framing device (close of build narrative → reopen as current-state lede), not redundancy; the apparent repetition is load-bearing per the spec lock and was explicitly flagged for verbatim shipping during the build kickoff. Hero photo (`public/projects/sofabot/sofabot.png`, 684×657, ~1.041 ratio, near-square) wired via `next/image` `priority` with `aspectRatio: '1/1'` override - second use of the prop on the site (HackZurich was first in step 9), same justification shape (16:10 default would crop ~35% vertical and defeat the photo; '1/1' preserves the sofa with ~4% horizontal crop only, no vertical loss). `ProjectHeader` component unchanged. Reused step-7/8/9/10 components verbatim (ProjectHeader, ProjectPageFooter, SectionDivider, RevealOnScroll, Breadcrumb); zero new components, zero new utility classes, zero new tokens. **`RoleBulletList` intentionally NOT imported** - the compressed pattern has no role bullet list. No inline figure (none specced for SofaBot). Section eyebrow source casing follows the standing rule (`§ 01 · Robotics · Club · Since 2019` middot for the multi-component type label, `§ 02 - The build` / `§ 03 - Outcome` hyphen-minus per the site-wide deviation). JSON-LD `BreadcrumbList` + `CreativeWork` shipped inline (deviation, same shape as NASA / T&S / HackZurich / Nicular / projects-index); `dateCreated: "2019-09"`, `contributor: { '@type': 'Organization', name: 'UNT Robotics Club' }`. Bare-title pattern: `<title>` renders as `SofaBot - Alex Bacallao` per spec § 11.6. Elementary-school outreach mention is **not** on this page (compressed pattern has no role section to put it in anyway, and the spec § 11.10 amendment dropped the line because Alex wasn't personally at the visit); the same drop-rule will need to be applied to `pass2-resume.md` § 07 source markdown when step 13 (Resume page) lands. The `app/_data/projects.ts` SofaBot entry was already locked with the post-Pass-2-amendment `typeLabel: 'Robotics · Club · Since 2019'` (verified line 89 before the build); no `projects.ts` edit needed. Post-commit Level 3 review findings recorded below. |
| 12 | About | DONE | First content-page-pattern surface on the site (SITE_SPEC § 1.9.6) live at `/about`. Header (§ 01) + three prose body sections (§ 02 The career / § 03 A pinch of life experience / § 04 What I'm looking for) + global footer; no case-study scaffolding (no ProjectHeader, no RoleBulletList, no ProjectPageFooter, no Breadcrumb, no inline figures). All copy verbatim from `docs/pass2-about.md`. **Wink budget: ONE protected wink** per pass2-about.md § 5.9 (`"Enough to hold a real conversation, but not enough to write code reviews"` in § 03 paragraph 1). SITE_SPEC § 1.6 voice rule 5 lists About in the two-wink genre; the locked pass2-about.md spec wins. Drift logged as a Site-Wide Deviation in `deviations.md`. **Throughline:** § 04 paragraph 1 ships `"systems where reliability matters and where the work has tangible consequences"` verbatim per spec § 5.5; the Resume page (step 13) ships its own locked phrase (`"shipping software to non-developers who need it to just work"`) per pass2-resume.md § 6.3. The SITE_SPEC § 1.11 verbatim-share claim is a separate Site-Wide Deviation. **Privacy carve-out honored** (SITE_SPEC § 1.9.11): no family-origin or ethnicity detail; Spanish capability ships as a professional fact only (`"I speak Spanish and English natively, plus German at A2/B1 from three years of school"`). Source resume markdown not consulted. Numerals-vs-determiners distinction respected: `A2/B1` (literal CEFR), `three years of school` (spelled per spec § 5.10), `one Swiss, one German, one Italian, one Egyptian, and me` (enumeration determiner). Illustrative-fragment cadence in § 03 ships verbatim (`"Quick asides between the two German speakers, the occasional clarification."`); § 04 paragraph 2 `"Full-time."` one-word sentence ships verbatim. **Zero new components introduced.** Reused four existing primitives: `SectionDivider` (between sections, 80px desktop / 60px mobile breathing room), `RevealOnScroll` (wraps § 02-§ 04; § 01 hero stays at full opacity per spec § 5.8), `identity` data (not directly imported here - the Person JSON-LD inlines values verbatim from spec § 5.7), inlined `SHELL` constant (`'max-w-[880px] mx-auto px-5 md:px-8'` - 5+ call sites now; intentionally not extracted per the abstraction-threshold rule, revisit at step 18 if a global tweak is needed). No `RoleBulletList`, no `ProjectHeader`, no `ProjectPageFooter`, no `Breadcrumb`. Section eyebrow source casing follows the standing site-wide rule (natural casing, hyphen-minus): `§ 01 - About`, `§ 02 - The career`, `§ 03 - A pinch of life experience`, `§ 04 - What I'm looking for`; `mono-label` CSS produces the visual ALL-CAPS. JSON-LD `AboutPage` with `Person` mainEntity shipped inline (deviation, same shape as steps 6-11); email intentionally omitted per pass2-about.md § 5.7 note (only `/contact` exposes email). Bare-title pattern matches steps 8-11; `<title>` renders as `About - Alex Bacallao`. Post-commit Level 3 review findings recorded below. |
| 13 | Resume (HTML + PDF) | DONE | HTML page live at `/resume`. Two-format pattern (SITE_SPEC § 1.9.7): HTML is canonical web version, PDF (`/public/resume.pdf`) is hand-maintained by Alex (standing pre-launch deliverable; not generated by this build). Both share a visible `UPDATED APR 2026` date stamp; drift between them is the maintenance hazard. All copy verbatim from `docs/pass2-resume.md`. **Wink budget: ZERO** per locked no-wink genre commitment (SITE_SPEC § 1.6 voice rule 5 / § 1.12.2 / spec § 6.13); voice still applies. Throughline phrase ships verbatim per spec § 6.3 (`"shipping software to non-developers who need it to just work"`); no new SITE_SPEC § 1.11 deviation entry needed (already logged in step 12). Education closing line `"Worked through college as a software developer at T&S Machines"` ships verbatim (load-bearing). 8-month gap between Role 3 (T&S earlier) and Role 1 (T&S Digital Specialist) intentionally unaddressed per spec § 6.4. New components: `app/resume/_components/MonoTable.tsx` (locally co-located, NOT promoted to `app/_components/`; 4 in-code callers on /resume; promote in step 14 if /contact imports it) and `app/resume/_components/RoleEntry.tsx` (locally co-located, work-history-specific shape). Existing component edit: `RoleBulletList.description` widened from `string` to `React.ReactNode` to enable shipping the spec § 6.8 HackZurich projects bullet's inline bold-italic on `1st place, Migros challenge.` verbatim - backwards-compatible with the 4 prior callers (NASA / T&S / HackZurich / Nicular My Role bullets all pass plain strings); not a deviation. Skills wall ships 4 mono tables verbatim from spec § 6.6: Programming languages (13 cells, 7 rows), Web & markup (7 items), Tools & platforms (11 cells with `Git · GitHub · GitLab` as one grouped chip), Spoken languages (3 items + visible `Language / Level` header). Awards section ships 3 entries via `RoleBulletList` (no `ledeHref`); Projects section ships 3 entries via `RoleBulletList` (with `ledeHref` on all three) plus closing prose paragraph naming Nicular and SofaBot inline (no links - per spec § 6.8). HackZurich entry intentionally repeats in § 06 Awards and § 07 Projects (cross-reference, not duplication; spec § 6.8 voice note confirms). Phone number excluded from HTML and JSON-LD (PDF only). Email is in top contact line (mailto link) but excluded from JSON-LD (sitewide privacy pattern). GitHub excluded from resume contact line (lives in global footer). PDF download affordance: `<a href="/resume.pdf" download>` (not Next.js `<Link>`); arrow `↓` is part of visible link text per spec § 6.2. Date stamp value: `Updated Apr 2026` in source (mono-label CSS uppercases to `UPDATED APR 2026`). Hyphen-minus throughout per the standing site-wide deviation. JSON-LD `ProfilePage` with `Person` mainEntity ships inline (deviation, same shape as steps 6-12); 3 awards verbatim, no email/telephone fields. Section eyebrow source casing follows the standing rule (natural casing, hyphen-minus): `§ 01 - Resume`, `§ 02 - Summary`, `§ 03 - Work`, `§ 04 - Education`, `§ 05 - Skills & languages`, `§ 06 - Awards`, `§ 07 - Projects`. Bare-title pattern: `<title>` renders as `Resume - Alex Bacallao`. Post-commit Level 3 review findings recorded below. |
| 14 | Contact | TODO | No-form, mailto-first pattern |
| 15 | SEO files | TODO | llms.txt, robots.txt, sitemap, JSON-LD per page |
| 16 | OG image generation | TODO | @vercel/og, single template |
| 17 | Em-dash linter | TODO | Build-time check, fail on U+2014 |
| 18 | Lighthouse optimization | TODO | LCP < 1.5s, CLS 0, INP < 200ms |
| 19 | Deploy | TODO | Domain setup, Vercel production |

## Components Built

| Component | Path | Used On |
|-----------|------|---------|
| RootLayout | app/layout.tsx | All pages (loads fonts, sets html/body shell, mounts Masthead + main + SiteFooter) |
| Masthead | app/_components/Masthead.tsx | All pages (wired in layout, step 4). Inner row constrained to 880px; outer header stays full-viewport-width for sticky bg + scroll hairline. |
| Footer | app/_components/Footer.tsx | All pages via SiteFooter wrapper. Two render modes; inner wrapper constrained to 880px. |
| SiteFooter | app/_components/SiteFooter.tsx | All pages. Tiny client wrapper that picks Footer mode by `usePathname()`: contact on `/`, sitewide elsewhere. |
| ProjectCard | app/_components/ProjectCard.tsx | Homepage and /projects index. Step 6 added an optional `caption` mode on the inner `ProjectImagePlaceholder` so the same component handles both deferred-photo placeholders and § 1.10.5 typographic placeholders. |
| RevealOnScroll | app/_components/RevealOnScroll.tsx | Homepage selected work cards + Now block; NASA, T&S, HackZurich, Nicular case study sections 02-05; SofaBot case study sections 02-03 (compressed pattern has only two scroll-fade-in sections). Reusable on any page that wants scroll-fade per section 2.7. |
| SectionDivider | app/_components/SectionDivider.tsx | Homepage, /projects index, NASA, T&S, HackZurich, Nicular, SofaBot case studies. Promoted from inline helpers on the first two pages. 1px `border-rule` line in the 880px shell, 60px (mobile) / 80px (desktop) of vertical breathing room. |
| Breadcrumb | app/_components/Breadcrumb.tsx | NASA, T&S, HackZurich, Nicular, SofaBot case studies. Locked structure per pass2-nasa-circadian-lighting.md § 3.1: `<nav aria-label="breadcrumb">` wrapping a real `<ol>`, ` / ` is text content (not a CSS pseudo-element), final item is non-link with `aria-current="page"`. |
| ProjectHeader | app/_components/ProjectHeader.tsx | NASA, T&S, HackZurich, Nicular, SofaBot case studies. Composes section 01 (breadcrumb + eyebrow + H1 + italic subtitle + dek + hero figure). Two hero modes: `image` (real photo via `next/image` with `priority`) and `placeholder` (typographic, for HIPAA / IP-constrained pages per § 1.10.5; **first real use is Nicular in step 10**). Step 9 added an optional `aspectRatio` prop (`'16/10' \| '4/3' \| '1/1'`; default `'16/10'`) on both hero modes for pages whose source can't tolerate the 16:10 crop. HackZurich (step 9) and SofaBot (step 11) both use `'1/1'`; NASA / T&S / Nicular omit the prop and continue to render `aspect-[16/10]`. The class is resolved through a small `ASPECT_CLASS` lookup map so Tailwind's JIT emits each arbitrary value as a literal. Step 10 also aligned a one-line className inside the placeholder branch (numeral color `text-text-secondary` -> `text-text-primary`) to spec § 10.1 on first real use; not logged as a deviation since the speculative branch never rendered before this commit. |
| RoleBulletList | app/_components/RoleBulletList.tsx | NASA, T&S, HackZurich, Nicular case studies, **and Resume (Awards § 06 + Projects § 07)** as of step 13. SITE_SPEC § 1.10.4 pattern: custom middot bullet in `accent`, bold Inter 500 lede with trailing period inside the bold run, regular-weight description follows immediately, no separator. Optional `ledeHref` upgrades the lede to a `<Link>` for the contact / resume variants. **Step 13 widened the `description` prop type from `string` to `React.ReactNode`** to support inline markup on the resume's HackZurich projects bullet (verbatim spec § 6.8 ships bold-italic `1st place, Migros challenge.` inside the description); backwards-compatible with all 4 prior callers (NASA / T&S / HackZurich / Nicular My Role bullets all pass plain strings, which remain a subtype of ReactNode). Not a deviation - normal implementation that enables shipping the locked spec verbatim. |
| ProjectPageFooter | app/_components/ProjectPageFooter.tsx | NASA, T&S, HackZurich, Nicular, SofaBot case studies. Two-column row above the global footer: `← Back to projects` (Next.js `<Link>` to `/projects`) on the left, `See contact ↓` (plain `<a href="#contact">` for same-page anchor) on the right. Stacks left-aligned on mobile. |
| identity (data) | app/_data/identity.ts | All pages, JSON-LD, llms.txt (when those steps land) |
| MonoTable | app/resume/_components/MonoTable.tsx | Resume page (4 mono tables in § 05 Skills and languages: Programming languages, Web & markup, Tools & platforms - all three with no visible header; Spoken languages with visible `Language` / `Level` header per SITE_SPEC § 1.10.2 because column meanings are asymmetric). Co-located locally because /resume is the only in-code caller today; the contact page (step 14) is spec'd to use the same pattern for its response-time-by-channel table, but until /contact actually imports the component, sitewide promotion is forward-prep. Promote to `app/_components/MonoTable.tsx` in step 14 if and when /contact imports it (one `git mv` + two import-path updates). Source casing natural; CSS `text-transform: uppercase` produces visual ALL-CAPS. Sr-only `<thead>` always present per docs/CLAUDE.md a11y rule. |
| RoleEntry | app/resume/_components/RoleEntry.tsx | Resume page only (3 callers in § 03 Work: T&S Digital Specialist with case-study link to /projects/ts-machines, Nicular without link, T&S earlier role without link). Locked structure per SITE_SPEC § 1.9.10 / spec § 6.4: two-line structured header (company-role line in Fraunces 22px with date range mono right-aligned + location line in Inter 14px text-secondary), prose body via children, optional case-study link as Inter 500 14px accent-color row with arrow as part of visible link text. Stacks on mobile (<768px). Co-located locally because no other page on the site uses this work-history-specific shape today. Education § 04 deliberately does NOT use this component - the degree title would render italic same as a role title and that's a semantic mismatch (a degree is not a role); Education's header markup is inlined at the call site instead. |
| projects (data) | app/_data/projects.ts | Homepage selected work and /projects index. Now exports `allProjects` (5 entries, locked quality-weighted order) plus `homepageProjects = allProjects.slice(0, 3)`. Optional `image` and `placeholderCaption` fields select the card's hero render mode. |

## Pages Live

| Page | Route | Lighthouse Mobile | Last Tested |
|------|-------|-------------------|-------------|
| Homepage | / | (pending step 18) | 2026-04-29 (manual scroll, dev server) |
| Projects index | /projects | (pending step 18) | 2026-04-29 (manual scroll, dev server) |
| NASA Circadian Lighting | /projects/nasa-circadian-lighting | (pending step 18) | 2026-04-29 (curl + structural HTML check; full browser scroll pending) |
| T&S Machines | /projects/ts-machines | (pending step 18) | 2026-04-29 (manual scroll, dev server; hero crop pre-approved) |
| HackZurich Migros | /projects/hackzurich-migros | (pending step 18) | 2026-04-29 (curl + structural HTML check; full browser scroll pending user pass) |
| Nicular | /projects/nicular | (pending step 18) | 2026-04-29 (curl + structural HTML check; full browser scroll on the placeholder hero pending user pass) |
| SofaBot | /projects/sofabot | (pending step 18) | 2026-04-29 (curl + structural HTML check; full browser scroll on the 1:1 hero crop pending user visual pass to confirm both winks land tonally and the sofa subject is preserved) |
| About | /about | (pending step 18) | 2026-04-29 (curl + structural HTML check; full browser scroll on the prose-only visual rhythm pending user visual pass before push) |
| Resume | /resume | (pending step 18) | 2026-04-29 (curl + structural HTML check on title, em-dash count = 0, forbidden-word count = 0, JSON-LD ProfilePage block parses with 3 awards verbatim and no email/telephone fields, throughline phrase confirmed, load-bearing Education closing line confirmed, HackZurich projects bullet renders as `<strong class="font-semibold italic">1st place, Migros challenge.</strong>`, PDF download link has `download` attribute, mailto link present; full browser scroll on role entry header alignment + skills wall density + bold-italic visual rendering pending user visual pass before push) |

## Pending Items (Not in Build Order)

- [ ] Domain registration (alexbacallao.com or backup)
- [ ] Move reference images to /public/projects/[slug]/ paths
- [ ] Resume PDF (hand-maintained, ATS-parseable)
- [ ] GitHub presentability before launch
- [ ] Pre-launch real photo check (T&S, NASA, HackZurich, SofaBot confirmed; Nicular uses typographic placeholder by spec § 10.1, no photo coming). NASA hero wired in step 7; T&S hero wired in step 8; HackZurich hero + inline figure wired in step 9; Nicular placeholder wired in step 10; SofaBot hero wired in step 11. **All five project pages now render their final hero treatment.** `ProjectCard` on the homepage and /projects index still renders the deferred-photo placeholder for cards 01/02/03/05 (forward-prep `image` path encoded in `projects.ts` per the step-7 deviation; awaiting a separate photo-wiring step that switches the placeholder to `<Image />` across those card slots). The Nicular index card already renders the § 1.10.5 typographic placeholder (locked permanent state) and is not part of the photo-wiring step.
- [x] ~~Drop the elementary-school outreach line from `pass2-resume.md` § 07 source markdown (or its successor in step 13 build) per spec § 11.10 amendment.~~ **Closed by step 13 verbatim adherence to spec § 6.8** - the resume Projects section closing prose ships as `"Also: a HIPAA-compliant patient messaging system at Nicular (Vonage, GSuite, 2022), and SofaBot from the UNT robotics club."` with no outreach mention, matching the spec walkback per SITE_SPEC § 1.12.6 and the SofaBot project page (step 11).
- [ ] Skip-to-content link in masthead (a11y polish, not in spec but worth tracking)
- [ ] RevealOnScroll: elements already in viewport at mount sometimes don't fire the fade-in. Reproduces by loading the page with cards above-the-fold-ish; refresh fixes it. Likely fix: check element position on mount and reveal immediately if already intersecting. Not a launch blocker, but flag for a polish pass.
- [ ] NASA project page § 01 eyebrow source uses ALL-CAPS (`§ 01 · EMBEDDED · SENIOR DESIGN · SPRING 2022` in `app/projects/nasa-circadian-lighting/page.tsx`). Should align to natural source casing in a follow-up commit per the standing site-wide rule (visual rendering is identical because `mono-label` applies `text-transform: uppercase`; this is a source-fidelity cleanup, not a visual change). Discovered during step 8 build; T&S shipped with natural casing from the start.

## Review Findings

(Entries added here after each post-build review pass. Format: dated section with open items.)

### 2026-04-29 - Step 7 Level 3 review (NASA Circadian Lighting)

Findings only; nothing fixed during review per CLAUDE.md.

**Content review.** All copy verbatim from `docs/pass2-nasa-circadian-lighting.md`. Single H1 ("NASA Circadian Lighting"), four H2s in sentence case ("The problem", "Approach", "My role", "Outcome"). Zero em-dashes anywhere in the rendered HTML (`grep '—' /tmp/nasa.html` returned 0). No forbidden words. Wink budget honored: two protected asides (dek + outcome), spec § 3.7 grants two for this page. Bullet list ledes end with period inside the bold run as specified.

**SEO review.** Title tag (`NASA Circadian Lighting - Alex Bacallao`), meta description, canonical (`https://alexbacallao.com/projects/nasa-circadian-lighting`), OG title/description/url/type, all present in the metadata export. Two JSON-LD blocks (`BreadcrumbList` + `CreativeWork`) confirmed in source. Hero image has alt text. No other images on the page.

**Design review.** Fraunces / Inter / JetBrains Mono used per spec. Color tokens used throughout (`text-text-primary`, `text-text-secondary`, `text-text-muted`, `text-accent`, `bg-border-rule`, `border-border-hairline`). No hardcoded hex values outside `globals.css`. Hero photo: 0.5px hairline outline, 16:10 container, no shadow, no rounded corners. Caption uses `mono-label` 11px in `text-text-secondary` with the `FIG. 0N` format.

**Cross-page sync.** This page does not render project cards. `app/_data/projects.ts` line 72 (`href: '/projects/nasa-circadian-lighting'`) matches the new route. JSON-LD `name` (`NASA Circadian Lighting`) matches `projects.ts` `title`. № 03 card on the homepage and on /projects already links here.

**Code quality.** No unused imports (verified by `npm run build` strict TS pass). No `any`. No console.log. No inline styles. No missing key props on list renders (Breadcrumb keys on `${label}-${index}`, RoleBulletList keys on `${lede}-${index}`). `prefers-reduced-motion` is respected by the existing `globals.css` block (covers `scroll-behavior`, `transition-duration`, `animation-*`) and by `RevealOnScroll`'s runtime `matchMedia` check.

**Responsive.** H1 36px / 56px, H2 26px / 32px (from globals.css element defaults). `ProjectPageFooter` stacks vertically below 768px. No masthead changes.

**Build and performance.** `npm run build` passes with zero warnings; the new route prerenders as static content. Lighthouse run is deferred to step 18 per the build order; targets remain LCP < 1.5s, CLS 0, INP < 200ms. The hero `<Image priority>` with explicit `width={1280}` `height={800}` reserves the layout box; CLS should stay 0.

**Open items / decisions to record (no fixes):**
- Hero photo is 4000×2252 (16:9). Container is `aspect-[16/10]` with `object-cover`, so ~6% horizontal crop on the source. Pre-approved before commit; flag for visual confirmation when Lighthouse / browser pass runs in step 18.
- `next dev` server was already running on port 3001 from a prior session. The dev-check ran against 3001 (HTTP 200 on `/projects/nasa-circadian-lighting`, `/`, `/projects`, and the `/_next/image` optimizer for the hero). A direct browser scroll across all four breakpoints is still owed before crossing the page off the user-memory "browser-check before declaring UI done" rule.
- Smooth-scroll anchor (`See contact ↓`) lands the dark global footer just under the sticky masthead. No `scroll-margin-top` compensation; the dark band is visually obvious so this is acceptable. Re-check after the visual browser pass.

### 2026-04-29 - Step 8 Level 3 review (T&S Machines)

Findings only; nothing fixed during review per CLAUDE.md.

**Content review.** All copy verbatim from `docs/pass2-ts-machines.md`. Single H1 (`CNC software at T&S Machines`); five H2s in sentence case across § 02-§ 05 plus § 03's `The Docker side-project`. Five paragraphs in § 02, two in § 03, lede + six bullets + closing scope in § 04, three paragraphs in § 05. Zero em-dashes in rendered HTML (`grep $'\xe2\x80\x94' /tmp/ts.html` returned 0). No forbidden words. Wink budget honored: zero winks, matching the locked no-wink genre commitment for professional case studies (SITE_SPEC § 1.12.2). The § 02 paragraph 5 closing line about the math-issue patched on a customer machine ships verbatim per spec § 7.7 - professional candor, not a wink. Bullet ledes end with period inside the bold run as specified. `side-project` hyphenated throughout this page as the locked local exception (SITE_SPEC § 1.6 rule 12 / spec § 7.3); two-word `side project` is unaffected elsewhere on the site.

**SEO review.** Title tag (`CNC software at T&S Machines - Alex Bacallao`) renders correctly via the bare-title-plus-template approach; matches spec § 7.8 exactly. Meta description, canonical (`https://alexbacallao.com/projects/ts-machines`), OG title / description / url / type all present and correct. Two JSON-LD blocks (`BreadcrumbList` + `CreativeWork`) confirmed in source. Hero image has alt text. No other images on the page.

**Title-template fix verification.** All four prerendered routes now match their spec titles:
- `/` → `Alex Bacallao - Software Developer` (absolute, opts out of template).
- `/projects` → `Projects - Alex Bacallao` (template-suffixed).
- `/projects/nasa-circadian-lighting` → `NASA Circadian Lighting - Alex Bacallao` (template-suffixed).
- `/projects/ts-machines` → `CNC software at T&S Machines - Alex Bacallao` (template-suffixed).

**Design review.** Fraunces / Inter / JetBrains Mono used per spec. Color tokens used throughout (`text-text-primary`, `text-text-secondary`, `text-text-muted`, `text-accent`, `bg-border-rule`, `border-border-hairline`). No hardcoded hex values introduced. Hero photo: 0.5px hairline outline, 16:10 container, no shadow, no rounded corners. Caption uses `mono-label` 11px in `text-text-secondary` with the `FIG. 0N` format. Section eyebrow source is natural casing (`§ 01 · Industrial · Software · Since 2019`, `§ 02 - The work`, etc.); `mono-label` CSS produces the visual ALL-CAPS. First project page on the site that follows the standing source-casing rule from the start.

**Cross-page sync.** This page does not render project cards. `app/_data/projects.ts` line 50 (`href: '/projects/ts-machines'`) matches the new route. JSON-LD `name` (`CNC software at T&S Machines`) matches `projects.ts` `title`. № 01 card on the homepage and on /projects already linked here; the destination is now real.

**Code quality.** No unused imports (verified by `npm run build` strict TS pass on Next.js 16.2.4 + Turbopack). No `any`. No console.log. No inline styles. No missing key props on list renders (RoleBulletList keys on `${lede}-${index}`). `prefers-reduced-motion` is respected by the existing `globals.css` block (covers `scroll-behavior`, `transition-duration`, `animation-*`) and by `RevealOnScroll`'s runtime `matchMedia` check. Zero new components: ProjectHeader, RoleBulletList, ProjectPageFooter, SectionDivider, RevealOnScroll all reused verbatim from step 7.

**Responsive.** H1 36px / 56px, H2 26px / 32px (from `globals.css` element defaults). `ProjectPageFooter` stacks vertically below 768px. No masthead changes.

**Build and performance.** `npm run build` passes with zero warnings; the new route prerenders as static content. Lighthouse run is deferred to step 18 per the build order; targets remain LCP < 1.5s, CLS 0, INP < 200ms. The hero `<Image priority>` with explicit `width={1280}` `height={800}` reserves the layout box; CLS should stay 0.

**Open items / decisions to record (no fixes):**
- Hero photo source is 4032×3024 (4:3); container `aspect-[16/10]` with `object-cover` produces ~16.7% combined vertical crop (8.3% top, 8.3% bottom). Pre-approved during the step-8 dev-server browser pass; subject (the machine on the production floor) is preserved at the 16:10 crop. Flag for visual confirmation during the step 18 Lighthouse / browser pass.
- Title-template double-suffix fix bundled into this commit affected three pre-existing pages (homepage, /projects, NASA). Bundling decision logged in the per-`/projects/ts-machines` Deviations entry; the rule's edge case (typo-class systemic bug discovered while building this page; identical fix shape; one of the four call sites is squarely inside step 8) justified bundling over a separate cleanup commit.
- NASA's § 01 eyebrow source is ALL-CAPS (`§ 01 · EMBEDDED · SENIOR DESIGN · SPRING 2022`) - inconsistent with the standing source-casing rule and with T&S's natural-casing source. Visual rendering is identical (mono-label uppercases). Logged as a Pending Items entry above; not silent-fixed in this commit per user instruction.
- `next dev` server was running on port 3001 from the prior session (PID 11324). The dev-check ran against 3001 (HTTP 200 on `/projects/ts-machines`, all four titles verified via curl).

### 2026-04-29 - Step 9 Level 3 review (HackZurich Migros)

Findings only; nothing fixed during review per CLAUDE.md.

**Content review.** All copy verbatim from `docs/pass2-hackzurich-migros.md`. Single H1 (`Supply chain rerouting at HackZurich`); five H2s in sentence case (`The problem`, `The approach`, `My role`, `Outcome`). Two paragraphs in § 02, paragraph + inline figure + paragraph in § 03, lede + 5 bullets + closing scope in § 04, four paragraphs in § 05. Zero em-dashes in rendered HTML (`grep $'\xe2\x80\x94' /tmp/hz.html` returned 0). No forbidden words. Wink budget honored: zero winks per the locked no-wink genre commitment for professional case studies (SITE_SPEC § 1.12.2). The closing paragraph's "great time, one of the more memorable experiences" beat reads as colloquial sincerity, not a wink, per spec § 8.7. The illustrative-fragment cadence in § 05 paragraph 4 (`Different working styles, different default assumptions, the occasional language gap. We made it work.`) ships verbatim per spec § 8.5 and SITE_SPEC § 1.6 voice rule 10. Date is **September 2022** (JSON-LD `dateCreated: "2022-09"`). The "won the Migros challenge" precision (vs "won HackZurich overall") holds throughout body, JSON-LD `award`, and the dek.

**SEO review.** Title tag (`Supply chain rerouting at HackZurich - Alex Bacallao`) renders correctly via the bare-title-plus-template approach; matches spec § 8.8 exactly. Meta description, canonical (`https://alexbacallao.com/projects/hackzurich-migros`), OG title / description / url / type all present and correct. Two JSON-LD blocks (`BreadcrumbList` + `CreativeWork`) confirmed in source; CreativeWork includes the spec-locked `award: '1st place, Migros challenge, HackZurich 2022'` (first project page on the site to ship an award field). Both images have alt text; both alt strings drop the `FIG. 0N - ` prefix per the established convention (logged in deviations).

**Design review.** Fraunces / Inter / JetBrains Mono used per spec. Color tokens used throughout (`text-text-primary`, `text-text-secondary`, `text-text-muted`, `text-accent`, `bg-border-rule`, `border-border-hairline`). No hardcoded hex values. Hero photo: `aspect-[1/1]` (per the new `aspectRatio: '1/1'` override on `ProjectHeader`), 0.5px hairline outline, no shadow, no rounded corners. Inline figure: `aspect-[16/10]` (16:10 native to the source 1184×746), 0.5px hairline, `my-8` (32px top + bottom) margin between paragraphs per spec § 8.3. Both figcaptions use `mono-label` 11px in `text-text-secondary` with the `FIG. 0N` format. Section eyebrow source casing: natural (`§ 01 · Web · Hackathon · 2022`, `§ 02 - The problem`, etc.) per the standing site-wide rule.

**Cross-page sync.** This page does not render project cards. `app/_data/projects.ts` line 61 (`href: '/projects/hackzurich-migros'`) matches the new route. JSON-LD `name` matches `projects.ts` `title`. № 02 card on homepage and /projects already linked here; the destination is now real. The `problem` line in `projects.ts` correctly says "won the Migros challenge", consistent with the page body's precision. **NASA / T&S regression check passed:** post-build curl on `/projects/nasa-circadian-lighting` and `/projects/ts-machines` both still serve `aspect-[16/10]` in the rendered hero className (the new `aspectRatio` prop is optional; both pages omit it and fall through to the `'16/10'` default in the `ASPECT_CLASS` lookup).

**Code quality.** No unused imports (verified by `npm run build` strict TS pass on Next.js 16.2.4 + Turbopack). No `any`. No console.log. No inline styles. Key props on the bullet list (RoleBulletList keys on `${lede}-${index}`). `prefers-reduced-motion` is respected by the existing `globals.css` block and by `RevealOnScroll`'s runtime `matchMedia` check. Component change: added `aspectRatio` prop to `ProjectHeader` with a tight type union (`'16/10' | '4/3' | '1/1'`) per the abstraction-threshold rule; the original four-option draft had a speculative `'4/5'` that was trimmed before commit because no caller exists. Inline figure not extracted to a shared component for the same reason (one occurrence across all five case studies).

**Responsive.** H1 36px / 56px, H2 26px / 32px (from `globals.css` element defaults). `ProjectPageFooter` stacks vertically below 768px. Inline figure scales fluidly via `sizes="(min-width: 880px) 880px, calc(100vw - 40px)"`.

**Build and performance.** `npm run build` passes with zero warnings; the new route prerenders as static. Lighthouse run is deferred to step 18 per the build order; targets remain LCP < 1.5s, CLS 0, INP < 200ms. The hero `<Image priority>` with `width={877}` `height={853}` reserves the layout box; CLS should stay 0 even with the `aspect-[1/1]` override (the lookup-map approach guarantees Tailwind emits the class as a literal).

**Hook fired path.** Post-commit hook produced Level 3 `additionalContext` per the d39d906 chained-command fix - chained `git add ... && git commit` now fires correctly (verified during step 9 commit). The Level 3 review work was preempted by writing this block into the same commit, matching the NASA / T&S precedent. The hook's checklist items not addressed in-commit (Lighthouse run) are deferred to step 18 per CLAUDE.md.

**Open items / decisions to record (no fixes):**
- Hero photo source is 877×853 (~1:1); container `aspect-[1/1]` with `object-cover` produces ~2.7% horizontal crop only, no vertical loss. All 5 team members visible. The decision to use `'1/1'` instead of `'16/10'` was made via AskUserQuestion before the build, not after. Flag for visual confirmation during the user's manual browser pass.
- Inline figure source is 1184×746 (~1.587, near-native 16:10); `aspect-[16/10]` produces ~0.8% horizontal crop. Subject (the route view) is preserved.
- NASA's § 01 eyebrow source is still ALL-CAPS (`§ 01 · EMBEDDED · SENIOR DESIGN · SPRING 2022`); flagged in Pending Items as a follow-up cleanup, not addressed in this commit.

### 2026-04-29 - Step 10 Level 3 review (Nicular)

Findings only; nothing fixed during review per CLAUDE.md.

**Content review.** All copy verbatim from `docs/pass2-nicular.md`. Single H1 (`Patient messaging at Nicular`); four H2s in sentence case (`The problem`, `The approach`, `My role`, `Outcome`). Two paragraphs in § 02, two paragraphs in § 03, lede + 4 bullets + closing scope in § 04, two paragraphs in § 05. Zero em-dashes in rendered HTML (`grep $'\xe2\x80\x94' /tmp/nicular.html` returned 0). No forbidden words. Wink budget honored: zero winks per the locked no-wink genre commitment for professional case studies (SITE_SPEC § 1.12.2). The mixed-depth disclaimer line in § 03 paragraph 2 (`"Some of that was real configuration work for the integrations I owned; some was just calling the right framework methods correctly."`) ships verbatim per spec § 10.3 voice notes and SITE_SPEC § 1.12.4 - that is honest scope framing, not a wink. The "Two months total tenure" line in § 04 lede and the "I came back to T&S after two months" line in § 05 paragraph 2 are the only mentions of the transition; no "why I left" framing per SITE_SPEC § 1.12.3. Bullet ledes end with period inside the bold run. Date alignment: eyebrow text reads "Late 2022", JSON-LD `dateCreated` is `"2022-11"` per spec § 10.8 schema 2 (verbatim).

**SEO review.** Title tag (`Patient messaging at Nicular - Alex Bacallao`) renders correctly via the bare-title-plus-template approach; matches spec § 10.8 exactly. Meta description, canonical (`https://alexbacallao.com/projects/nicular`), OG title / description / url / type all present and correct. Two JSON-LD blocks (`BreadcrumbList` + `CreativeWork`) confirmed in source via `grep -oE '<script type="application/ld\+json"' /tmp/nicular.html` (count = 2). The placeholder `<div role="img">` carries an auto-constructed `aria-label` of `"№ 04 placeholder, HIPAA-compliant · No customer data shown"`; the figcaption beneath the placeholder reads `FIG. 01 - No image. The work was patient communication under HIPAA; nothing displayable was ours to show.` (hyphen-minus per site-wide deviation). No `<img>` or `<Image>` for the hero - the typographic placeholder is the visual; existing 100%-image-alt-coverage rule unaffected (no images on the page).

**Title-template regression check (all five pages).** All five prerendered routes still render their spec titles after the step 10 commit:
- `/` → `Alex Bacallao - Software Developer`
- `/projects` → `Projects - Alex Bacallao`
- `/projects/nasa-circadian-lighting` → `NASA Circadian Lighting - Alex Bacallao`
- `/projects/ts-machines` → `CNC software at T&S Machines - Alex Bacallao`
- `/projects/hackzurich-migros` → `Supply chain rerouting at HackZurich - Alex Bacallao`
- `/projects/nicular` → `Patient messaging at Nicular - Alex Bacallao`

**Design review.** Fraunces / Inter / JetBrains Mono used per spec. Color tokens used throughout (`text-text-primary`, `text-text-secondary`, `text-text-muted`, `text-accent`, `bg-bg-paper`, `border-border-hairline`, `bg-border-rule`). No hardcoded hex values. Typographic placeholder hero: 0.5px hairline outline, `aspect-[16/10]` container, no shadow, no rounded corners; 88px Fraunces "№ 04" (NUMERO SIGN U+2116) centered in `text-text-primary` per spec § 10.1; mono caption `HIPAA-compliant · No customer data shown` beneath in `text-text-muted` with `tracking-[0.1em]`, uppercased visually by `mono-label` CSS. Section eyebrows on § 02-§ 05 use hyphen-minus (U+002D) per the site-wide deviation; § 01 eyebrow `§ 01 · Healthcare · Full-time · Late 2022` uses U+00B7 middot separators (multi-component type label).

**Cross-page sync.** This page does not render project cards. `app/_data/projects.ts` line 83 (`href: '/projects/nicular'`) matches the new route. JSON-LD `name: "Patient messaging at Nicular"` matches `projects.ts` `title`. № 04 card on /projects already linked here; the destination is now real. The placeholder caption string on the page hero (`'HIPAA-compliant · No customer data shown'`) is identical to the value at `app/_data/projects.ts` line 84 — same source string flows into both surfaces, so any future copy edit propagates by changing one place. The `/projects` index card placeholder retains `text-text-secondary` for its numeral; **intentionally different from the page hero** per SITE_SPEC § 1.10.1 (the card pattern doesn't pin numeral color). **NASA / T&S / HackZurich regression check passed:** post-build curl on each renders the expected hero aspect class (NASA `aspect-[16/10]`, T&S `aspect-[16/10]`, HackZurich `aspect-[1/1]`). The `ProjectHeader` placeholder-branch numeral-color edit only touches the `mode: 'placeholder'` branch className; the three image-mode pages are byte-stable through that change.

**Code quality.** No unused imports (verified by `npm run build` strict TS pass on Next.js 16.2.4 + Turbopack). No `any`. No console.log. No inline styles. Key props on the bullet list (RoleBulletList keys on `${lede}-${index}`). `prefers-reduced-motion` is respected by the existing `globals.css` block and by `RevealOnScroll`'s runtime `matchMedia` check. Zero new components introduced this step; the page reuses the five-component set (ProjectHeader, RoleBulletList, ProjectPageFooter, SectionDivider, RevealOnScroll) from steps 7-9 verbatim. No new shared components, no new utility classes, no new tokens.

**Responsive.** H1 36px / 56px, H2 26px / 32px (from `globals.css` element defaults). `ProjectPageFooter` stacks vertically below 768px. Placeholder numeral scales from 64px to 88px at the 768px breakpoint (`text-[64px] md:text-[88px]`).

**Build and performance.** `npm run build` passes with zero warnings; the new route prerenders as static (verified in build output: `○ /projects/nicular`). Lighthouse run is deferred to step 18 per the build order; targets remain LCP < 1.5s, CLS 0, INP < 200ms. The placeholder is a styled `<div>` (no image network request, no LCP candidate), so the hero box reserves layout immediately - CLS should stay 0 with margin to spare.

**Hook fired path.** Post-commit hook should fire Level 3 (new project page) given the d39d906 chained-command fix; `app/projects/nicular/page.tsx` matches the new-page regex. The Level 3 review work was preempted by writing this block into the same commit, matching the NASA / T&S / HackZurich precedent. The hook's checklist items not addressed in-commit (Lighthouse run; full browser pass on the placeholder hero across all four breakpoints) are deferred to step 18 and to the user's manual scroll pass per CLAUDE.md.

**Open items / decisions to record (no fixes):**
- **ProjectHeader placeholder-branch numeral color was aligned to spec on first real use.** The speculative branch shipped in step 7 with `text-text-secondary` on the 88px Fraunces numeral; pass2-nicular.md § 10.1 specifies `text-primary`. Step 10 is the first real caller of the placeholder branch, so the className was edited to `text-text-primary` before the page rendered the placeholder for any user. This is bundled into the step 10 commit, captured in the commit message body, and not logged as a deviation - the speculative class was a spec-bug in a never-rendered code path; aligning to spec on first use is normal implementation, not a divergence. Verified post-build via `curl http://localhost:3000/projects/nicular | grep -oP '<span[^>]*text-text-primary[^"]*"[^>]*>№ 04</span>'`. The /projects index Nicular card placeholder (`ProjectCard.tsx`) retains `text-text-secondary` for its numeral; SITE_SPEC § 1.10.1 does not pin the placeholder numeral color, so the card-vs-hero color difference is not a spec violation, and not bundled into this commit.
- Dev server check ran against port 3000 (existing background `next dev` instance from a prior session, PID 28292 reported by the duplicate-spawn attempt). HTTP 200 on `/projects/nicular`, on the four other prerendered routes, and on `/`. A direct browser scroll across all four breakpoints is still owed before crossing the page off the user-memory "browser-check before declaring UI done" rule. The placeholder rendering visual matters more than usual for this commit; user is expected to confirm the hero treatment in a live browser pass.
- Spec § 10.8 schema 2 ships `dateCreated: "2022-11"` despite the eyebrow text reading "Late 2022". Used the spec value verbatim; flagged here so the discrepancy stays discoverable if anyone later updates one without the other.

### 2026-04-29 - Step 11 Level 3 review (SofaBot)

Findings only; nothing fixed during review per CLAUDE.md.

**Content review.** All copy verbatim from `docs/pass2-sofabot.md`. Single H1 (`SofaBot`); two H2s in sentence case (`The build`, `Outcome`). Three paragraphs in § 02, three paragraphs in § 03; subtitle and dek in § 01. Total ~515 words, the right shape for the hobbyist genre per spec § 11.0 (~235 in The build, ~210 in Outcome, plus subtitle + dek). Zero em-dashes in rendered HTML (`grep $'\xe2\x80\x94' /tmp/sofabot.html` returned 0). Zero forbidden words across the SITE_SPEC § 1.6 list (`passionate`, `thrive`, `leverage`, `synergy`, `cutting-edge`, `innovative`, `world-class`, `rockstar`, `ninja`, `guru`, `excited`, `thrilled`, `journey` — all 0 hits). **Wink budget honored: exactly two winks, both verbatim, well-separated.** Wink #1 (subtitle, wink-as-noun-phrase: `"a free couch and a stripped-for-parts go-kart"`) and wink #2 (§ 03 paragraph 1 closer, wink-by-understatement: `"The sofa drove, in a parade."`) each appear once in the rendered HTML and ship without modification, repositioning, or softening commentary. The page's other tonal choices (`"SofaBot at rest"` figure caption, `"We never got there."` illustrative fragment, `"(jobs, family, the regular set)"` parenthetical aside, `"on tank controls"` flat statement at the close of § 02 paragraph 2, `"progressing when we have the time"` closer of § 03) are voice, not winks - they ship verbatim per spec § 11.5. **The COVID `"hard pause"` bookend ships verbatim across the § 02/§ 03 seam** (close of § 02 paragraph 3: `"Then COVID hit in 2020, and the project went on hard pause"`; open of § 03 paragraph 3: `"The project went on hard pause for COVID in 2020 and has been on slow-burner status since 2022..."`). The apparent redundancy is a deliberate framing device per the locked spec; both halves ship as written, no deduplication, both verified present in the rendered HTML.

**SEO review.** Title tag (`SofaBot - Alex Bacallao`) renders correctly via the bare-title-plus-template approach; matches spec § 11.6 exactly. Meta description (159 chars), canonical (`https://alexbacallao.com/projects/sofabot`), OG title / description / url / type all present and correct. Two JSON-LD blocks (`BreadcrumbList` + `CreativeWork`) confirmed in source via `grep -oE '<script type="application/ld\+json"' /tmp/sofabot.html | wc -l` (count = 2; the earlier line-mode `grep -c` showed 1 because Next.js renders both script tags on a single line in dev — a regex-occurrence count is the correct verification, established here for future case-study commits). CreativeWork ships `dateCreated: "2019-09"` and `contributor: { '@type': 'Organization', name: 'UNT Robotics Club' }` per spec § 11.6 schema 2. Hero image has alt text (`'SofaBot at rest, somewhere on the UNT campus.'`), drops the `FIG. 01 - ` prefix per the established convention.

**Title-template regression check (all six pages).** All six prerendered routes still render their spec titles after the step 11 commit:
- `/` → `Alex Bacallao - Software Developer`
- `/projects` → `Projects - Alex Bacallao`
- `/projects/ts-machines` → `CNC software at T&S Machines - Alex Bacallao`
- `/projects/hackzurich-migros` → `Supply chain rerouting at HackZurich - Alex Bacallao`
- `/projects/nasa-circadian-lighting` → `NASA Circadian Lighting - Alex Bacallao`
- `/projects/nicular` → `Patient messaging at Nicular - Alex Bacallao`
- `/projects/sofabot` → `SofaBot - Alex Bacallao`

**Aspect-ratio regression check (all five project pages).** Verified post-build via curl + grep: NASA `aspect-[16/10]`, T&S `aspect-[16/10]`, HackZurich `aspect-[1/1]` (hero) plus `aspect-[16/10]` (inline figure), Nicular `aspect-[16/10]` (placeholder), SofaBot `aspect-[1/1]` (hero). Only HackZurich and SofaBot use the `aspectRatio` override, both at `'1/1'`; the other three omit the prop and fall through to the default. The step-9 `ASPECT_CLASS` lookup map continues to emit each class as a Tailwind literal.

**Design review.** Fraunces / Inter / JetBrains Mono used per spec. Color tokens used throughout (`text-text-primary`, `text-text-secondary`, `text-text-muted`, `bg-border-rule`, `border-border-hairline`). No hardcoded hex values. Hero photo: `aspect-[1/1]` (per the `aspectRatio: '1/1'` override on `ProjectHeader`), 0.5px hairline outline, no shadow, no rounded corners. Caption uses `mono-label` 11px in `text-text-secondary` with the `FIG. 0N` format. Section eyebrow source casing: § 01 uses middot (U+00B7) for the multi-component type label (`§ 01 · Robotics · Club · Since 2019`); § 02 and § 03 use hyphen-minus (U+002D) per the standing site-wide deviation (`§ 02 - The build`, `§ 03 - Outcome`). Source casing throughout is natural; `mono-label` CSS produces the visual ALL-CAPS.

**Cross-page sync.** This page does not render project cards. `app/_data/projects.ts` line 94 (`href: '/projects/sofabot'`) matches the new route. JSON-LD `name: "SofaBot"` matches `projects.ts` `title`. № 05 card on /projects already linked here; the destination is now real. The `app/_data/projects.ts` SofaBot entry (`typeLabel: 'Robotics · Club · Since 2019'`) was already locked with the post-Pass-2 form before this commit; verified during pre-build, no edit required. The card-level `problem` line (`"Mobile sofa robot for the UNT Robotics Club. Programming, electronics, assembly. Yes, it actually moved."`) is locked and out of scope for this commit.

**Code quality.** No unused imports (verified by `npm run build` strict TS pass on Next.js 16.2.4 + Turbopack; no warnings). No `any`. No console.log. No inline styles. `prefers-reduced-motion` is respected by the existing `globals.css` block and by `RevealOnScroll`'s runtime `matchMedia` check. Zero new components introduced this step; the page reuses the four-component set (ProjectHeader, ProjectPageFooter, SectionDivider, RevealOnScroll) from steps 7-10 verbatim. **`RoleBulletList` intentionally NOT imported** - the compressed pattern has no role bullet list. No inline figure (none specced for SofaBot). No new shared components, no new utility classes, no new tokens. Component change: zero - the `aspectRatio` prop on `ProjectHeader` and the `ASPECT_CLASS` lookup map already shipped in step 9; SofaBot is the second consumer of the prop, both at `'1/1'`.

**Responsive.** H1 36px / 56px, H2 26px / 32px (from `globals.css` element defaults). `ProjectPageFooter` stacks vertically below 768px. Hero scales fluidly via `sizes="(min-width: 880px) 880px, calc(100vw - 40px)"`.

**Build and performance.** `npm run build` passes with zero warnings; the new route prerenders as static (verified in build output: `○ /projects/sofabot`). Lighthouse run is deferred to step 18 per the build order; targets remain LCP < 1.5s, CLS 0, INP < 200ms. The hero `<Image priority>` with `width={684}` `height={657}` reserves the layout box; `aspect-[1/1]` on the rendered class guarantees the lookup-map literal; CLS should stay 0.

**Hook fired path.** Post-commit hook should fire Level 3 (new project page) given the d39d906 chained-command fix; `app/projects/sofabot/page.tsx` matches the new-page regex. The Level 3 review work was preempted by writing this block into the same commit, matching the NASA / T&S / HackZurich / Nicular precedent. The hook's checklist items not addressed in-commit (Lighthouse run; full browser pass on the 1:1 hero crop across all four breakpoints; visual confirmation that both winks land tonally) are deferred to step 18 and to the user's manual scroll pass per CLAUDE.md and the standing user directive on this commit.

**Open items / decisions to record (no fixes):**
- Hero photo source is **684×657** (PNG IHDR-verified, ratio 1.041, near-square); container `aspect-[1/1]` with `object-cover` produces ~4% horizontal crop only, no vertical loss. The decision to use `'1/1'` instead of the spec § 11.1 default `'16/10'` was made before the build, mirroring the HackZurich step-9 reasoning (16:10 default would crop ~35% vertical and defeat the photo). User flagged the hero crop as one of two visual-verification items for this commit; the curl-grep evidence confirms the class is rendered, but the visual confirmation that the sofa subject is preserved through the crop is owed during the user's browser pass.
- **Wink-tonality verification is owed.** Both winks ship verbatim per the curl-grep evidence (1 hit each on the wink-as-noun-phrase and wink-by-understatement strings); the user's standing directive for this commit is a visual / reading pass to confirm both land tonally on the rendered page rather than reading flat or stacked. Tracked here as a deferred item awaiting the browser pass; no in-commit fix.
- **First and only project page on the site with a non-zero wink budget.** Resume, T&S, HackZurich, Nicular are zero-wink genres per SITE_SPEC § 1.12.2. SofaBot is the hobbyist genre (per § 1.6 voice rule 5); the two-wink budget is per-page genre commitment, established in spec § 11.5. Future portfolio entries that reach the hobbyist genre will reference this page as the locked precedent.
- **Source elementary-school outreach mention deliberately not on this page.** The compressed pattern has no role section to put it in anyway; spec § 11.10 also explicitly drops the line because the user wasn't personally at the elementary-school visit. The same drop will need to be applied to the resume markdown when step 13 (Resume page) lands; tracked as a Pending Items entry above.
- Dev server check ran against port 3000 (existing background `next dev` instance from a prior session). HTTP 200 on `/projects/sofabot`, on the four other prerendered project routes, on `/projects`, and on `/`. A direct browser scroll across all four breakpoints is still owed before crossing the page off the user-memory "browser-check before declaring UI done" rule. User flagged the visual verification (1:1 hero crop preserves the sofa; both winks land tonally) as the gating items for this commit before push.

### 2026-04-29 - Step 12 Level 3 review (About)

Findings only; nothing fixed during review per CLAUDE.md.

**Content review.** All copy verbatim from `docs/pass2-about.md`. Single H1 (`About`); three H2s in sentence case across the three prose body sections (`The career`, `A pinch of life experience`, `What I'm looking for`). Three paragraphs in § 02, two paragraphs in § 03, two paragraphs in § 04. Total ~526 words per spec § 5.0 (above the Pass 1 minimum of 500). Zero em-dashes in rendered HTML (`grep $'\xe2\x80\x94' /tmp/about.html` returned 0). Zero forbidden words across the SITE_SPEC § 1.6 list (all 13 of `passionate`, `thrive`, `leverage`, `synergy`, `cutting-edge`, `innovative`, `world-class`, `rockstar`, `ninja`, `guru`, `excited`, `thrilled`, `journey` returned 0 hits). **Wink budget honored: exactly one wink** per pass2-about.md § 5.9 (`grep -ci "code reviews" /tmp/about.html` returned 1). The wink is *"Enough to hold a real conversation, but not enough to write code reviews"* in § 03 paragraph 1, second sentence. SITE_SPEC § 1.6 voice rule 5's two-wink-genre claim for About is logged as a Site-Wide Deviation; the locked pass2-about.md spec is authoritative for content. The illustrative fragment *"Quick asides between the two German speakers, the occasional clarification."* (§ 03 paragraph 1) and the one-word fragment *"Full-time."* (§ 04 paragraph 2) ship verbatim per spec § 5.4 / § 5.5 voice notes - illustrative cadence, not winks. The `Swiss grocery chain` line in § 04 paragraph 2 is descriptive accuracy (Migros), explicitly not a wink per spec § 5.5 voice note. The `Digital Specialist` job-title quote in § 02 paragraph 3 ships with straight ASCII double quotes (`&quot;`).

**Privacy carve-out (SITE_SPEC § 1.9.11) honored.** No family-origin or ethnicity detail anywhere in the rendered HTML. Spanish-language capability appears only in § 03 paragraph 1's professional-fact framing (`"I speak Spanish and English natively, plus German at A2/B1 from three years of school"`). Source resume markdown was not consulted for biographical detail during the build per the user's pre-commit directive. Carve-out is locked; not reversed without explicit user re-authorization.

**Cross-page narrative consistency.** § 04 paragraph 1 ships the spec-locked About throughline phrase verbatim: `"systems where reliability matters and where the work has tangible consequences"`. The Resume page (step 13, not yet shipped) will ship its own locked phrase from pass2-resume.md § 6.3 (`"shipping software to non-developers who need it to just work"`). The SITE_SPEC § 1.11 verbatim-share claim is incorrect; logged as a separate Site-Wide Deviation. Both pages share the throughline IDEA (audiences where reliability matters: CNC operators, clinicians, spacecraft crew) without sharing the same phrase. About's narrative voice can't absorb the Resume phrase without weakening the writing; Resume's phrase is locked verbatim from Alex.

**Numerals-vs-determiners distinction.** Spec § 5.10 codifies the rule for this page. Verified verbatim ship: `A2/B1` (literal CEFR levels, not a count - ships as the spec writes it), `three years of school` (spelled per spec, not as `3 years` - locked), `one Swiss, one German, one Italian, one Egyptian, and me` (enumeration determiner, spelled per § 1.6 rule 3). Quantities not present on the page; if any landed, they would use numerals (e.g. `4 years`).

**SEO review.** Title tag (`About - Alex Bacallao`) renders correctly via the bare-title-plus-template approach (`title: 'About'` in `app/about/page.tsx` metadata; layout's `%s - Alex Bacallao` template appends the suffix once); matches spec § 5.7 exactly. Meta description (`Software developer in Ft Worth, TX. UNT Computer Engineering 2023. Native Spanish and English, plus German A2/B1. Looking for backend, infra, or related work.`, 158 chars), canonical (`https://alexbacallao.com/about`), OG title / description / url / type all present and correct. One JSON-LD block (`AboutPage` with `Person` mainEntity) confirmed via `grep -oE '<script type="application/ld\+json"' /tmp/about.html | wc -l` (count = 1; About has no breadcrumb because the content page pattern doesn't carry one). `Person` schema fields ship verbatim per spec § 5.7: `name: 'Alejandro Bacallao'`, `alternateName: 'Alex Bacallao'`, `jobTitle: 'Software Developer'`, `alumniOf` UNT, `knowsLanguage` English/Spanish/German with ISO codes, `homeLocation` Ft Worth/TX/US, `sameAs` LinkedIn + GitHub. **Email is intentionally omitted** per spec § 5.7 note + docs/CLAUDE.md privacy rule (only `/contact` exposes email in JSON-LD).

**Title-template regression check (all eight pages).** All eight prerendered routes still render their spec titles after the step 12 commit:
- `/` → `Alex Bacallao - Software Developer`
- `/projects` → `Projects - Alex Bacallao`
- `/projects/ts-machines` → `CNC software at T&S Machines - Alex Bacallao`
- `/projects/hackzurich-migros` → `Supply chain rerouting at HackZurich - Alex Bacallao`
- `/projects/nasa-circadian-lighting` → `NASA Circadian Lighting - Alex Bacallao`
- `/projects/nicular` → `Patient messaging at Nicular - Alex Bacallao`
- `/projects/sofabot` → `SofaBot - Alex Bacallao`
- `/about` → `About - Alex Bacallao` (NEW)

**Aspect-ratio regression check (all five project pages, no images on About).** Verified post-build via curl + grep: NASA `aspect-[16/10]`, T&S `aspect-[16/10]`, HackZurich `aspect-[1/1]` (hero) plus `aspect-[16/10]` (inline figure), Nicular `aspect-[16/10]` (placeholder), SofaBot `aspect-[1/1]` (hero). About has zero hero / inline figures by design (content page pattern); no aspect-ratio class to verify on this page.

**Design review.** Fraunces / Inter / JetBrains Mono used per spec. Color tokens used throughout (`text-text-primary`, `text-text-secondary`, `text-text-muted`, `bg-border-rule`, `border-border-hairline`). No hardcoded hex values. Body sections cap at `max-w-[65ch]` per spec § 5.3-5.5; dek caps at `max-w-[60ch]` per spec § 5.2. Paragraph spacing 16px via `flex flex-col gap-4` per spec § 5.3. Section eyebrow source casing follows the standing site-wide rule (natural casing, hyphen-minus): `§ 01 - About`, `§ 02 - The career`, `§ 03 - A pinch of life experience`, `§ 04 - What I'm looking for`. None of the four eyebrows is multi-component; all use hyphen-minus, no middot needed. `mono-label` CSS produces the visual ALL-CAPS.

**Cross-page sync.** This page does not render project cards. `app/_data/projects.ts` and `app/_data/identity.ts` are unchanged by this step (verified by inspection during planning; the page does not import either). The masthead nav in `app/_data/identity.ts:21` already routed to `/about` from launch; `usePathname()` in `Masthead` correctly applies `aria-current="page"` and `text-accent` to the About nav link on the new route (verified post-build via `grep -oE '<a[^>]*href="/about"[^>]*>About</a>' /tmp/about.html`: returns `<a aria-current="page" class="text-accent no-underline" href="/about">About</a>`).

**Code quality.** No unused imports (verified by `npm run build` strict TS pass on Next.js 16.2.4 + Turbopack; zero warnings). No `any`. No console.log. No inline styles. No new components introduced; the page reuses the two existing primitives (`SectionDivider`, `RevealOnScroll`) plus the inlined `SHELL` constant. `prefers-reduced-motion` is respected by `RevealOnScroll`'s runtime `matchMedia` check. Page returns a fragment per the locked Site-Wide Deviation (root layout owns `<main>`); no double-wrap. **Did NOT extract** a `ContentPageHeader` / `ContentSection` / `Section` wrapper component during this build, even though the four section bodies are structurally similar - About is the first content-page surface, and per the abstraction-threshold rule, Resume (step 13) and Contact (step 14) are the second/third callers and only then is extraction warranted. The inlined `SHELL` constant is now in 5+ call sites but intentionally not extracted; revisit at step 18 (Lighthouse pass) if a global shell tweak becomes necessary, or never.

**Responsive.** H1 36px / 56px, H2 26px / 32px (from `globals.css` element defaults). `RevealOnScroll` short-circuits when `(prefers-reduced-motion: reduce)` is set. Section dividers render at 80px desktop / 60px mobile vertical breathing room (`my-15 md:my-20` in `SectionDivider.tsx`). Body width tops out at 65ch; dek tops out at 60ch.

**Build and performance.** `npm run build` passes with zero warnings; the new route prerenders as static (`○ /about` in build output). Lighthouse run is deferred to step 18 per the build order; targets remain LCP < 1.5s, CLS 0, INP < 200ms. The page has zero images; LCP candidate is the H1 text node. CLS should stay 0 with margin to spare since the prose-only layout has no late-loading layout-shifters.

**Hook fired path.** Post-commit hook should fire Level 3 (new page) given the d39d906 chained-command fix; `app/about/page.tsx` matches the new-page regex. The Level 3 review work is preempted into the same commit, matching the steps 7-11 precedent. The hook's checklist items not addressed in-commit (Lighthouse run; full browser pass on the prose-only visual rhythm at all four breakpoints) are deferred to step 18 and to the user's manual scroll pass per CLAUDE.md and the standing user directive on this commit (do NOT push - surface the result for visual review first).

**Open items / decisions to record (no fixes):**
- **First content-page-pattern surface on the site.** SITE_SPEC § 1.9.6 pattern locked here for first real use. Resume (step 13) and Contact (step 14) may reuse the same shape; if they do, that becomes the second / third caller and the abstraction-threshold rule says extract a `ContentPageHeader` / `ContentSection` then. Not preempted in this build.
- **Two SITE_SPEC drifts surfaced and logged as separate Site-Wide Deviations.** § 1.11 throughline-phrase verbatim claim and § 1.6 voice rule 5 wink count for About. Both pages affected (About + Resume future) ship as locked; SITE_SPEC.md amendments deferred to a future consolidation pass. Logging each drift as its own entry (rather than bundled) so future consolidation passes can find them atomically.
- **Don't push after this commit.** User directive: surface the result for visual review first. About is the first content-page-pattern surface; the prose-only visual rhythm (eyebrow density, 65ch body cap, 80px/60px divider rhythm, RevealOnScroll cadence on § 02-§ 04, active-link styling on the masthead) is what matters most for the visual pass. Push only after explicit user approval.
- Dev server check ran against port 3000 (existing background `next dev` instance from a prior session). HTTP 200 on `/about`, on the seven other prerendered routes (homepage, /projects, all five project pages). A direct browser scroll across all four breakpoints is still owed before crossing the page off the user-memory "browser-check before declaring UI done" rule.
- Source resume markdown was not consulted for biographical detail during the build per the privacy carve-out (SITE_SPEC § 1.9.11). When step 13 (Resume page) lands, the locked pass2-resume.md is authoritative for content; the SofaBot elementary-school outreach drop already tracked under Pending Items still applies.

### 2026-04-29 - Step 13 Level 3 review (Resume)

Findings only; nothing fixed during review per CLAUDE.md.

**Content review.** All copy verbatim from `docs/pass2-resume.md`. Single H1 (`Resume`); six H2s in sentence case across § 02-§ 07 (`Summary`, `Work`, `Education`, `Skills and languages`, `Awards`, `Projects`). § 02 single ~150-word paragraph; § 03 three role entries (T&S Digital Specialist with case-study link, Nicular without link, T&S earlier without link) reverse-chronological with the locked Nov 2022 - Jan 2023 / Nov 2019 - Mar 2023 overlap intact (student worker doing both for two months per spec § 6.4); § 04 institution + degree + ~50-word paragraph with the load-bearing closing line `"Worked through college as a software developer at T&S Machines"` (verified `grep -c "Worked through college as a software developer" /tmp/resume.html` = 1); § 05 four mono tables with subheads (Programming languages 13 cells, Web & markup 7 cells, Tools & platforms 11 cells incl. one grouped chip, Spoken languages 3 rows + visible header); § 06 three RoleBulletList items reverse-chronological with the HackZurich entry first; § 07 three RoleBulletList items with `ledeHref` + closing prose naming Nicular (no link) and SofaBot (no link) verbatim. Zero em-dashes in rendered HTML (`grep -c $'\xe2\x80\x94' /tmp/resume.html` returned 0). Zero forbidden words across the SITE_SPEC § 1.6 list (`grep -ciE 'passionate|thrive|leverage|synergy|cutting-edge|innovative|world-class|rockstar|ninja|guru|excited|thrilled|journey' /tmp/resume.html` returned 0). **Wink budget honored: zero winks** per the locked no-wink genre commitment for Resume (SITE_SPEC § 1.6 voice rule 5 / § 1.12.2 / spec § 6.13). The dry-summary genre is intact: every sentence in § 02 does work, no asides, no levity. The HackZurich entry's `1st place, Migros challenge.` bold-italic emphasis on the Projects bullet is editorial weight on a hard fact (verified `grep -oE '<strong[^>]*italic[^>]*>1st place, Migros challenge[^<]*</strong>' /tmp/resume.html` returns the styled run with the period inside the styled markup), not a wink. The Education `"Worked through college..."` closing line is informational context (load-bearing), not a wink.

**Throughline phrase ships verbatim per spec § 6.3.** `grep -c 'shipping software to non-developers who need it to just work' /tmp/resume.html` returns 1. The Alex-locked phrase appears as sentence 2 of the § 02 Summary paragraph. The SITE_SPEC § 1.11 verbatim-share-with-About claim is already logged as a Site-Wide Deviation from step 12; no new entry needed for step 13. About ships its own narrative throughline (`"systems where reliability matters and where the work has tangible consequences"`); both pages share the throughline IDEA without sharing the same phrase.

**Privacy / contact-discipline review.** `grep -oE 'href="mailto:alex.bacallao1996@gmail.com"' /tmp/resume.html` returns the expected mailto link in the top contact line. `grep -ci '817-319-0274' /tmp/resume.html` returns 0 (phone correctly excluded from HTML). Phone correctly absent from JSON-LD (`telephone present: false`). Email correctly absent from JSON-LD (`email present: false`) per the sitewide privacy pattern. GitHub correctly absent from the resume contact line (`grep -E 'github.com/TacticalDoggo' /tmp/resume.html | grep -v 'sameAs'` matches only the JSON-LD `sameAs` entry, not the contact line — the contact line stays minimal). LinkedIn URL ships in the contact line as a same-tab link per docs/CLAUDE.md "External links open in the same tab" rule.

**SEO review.** Title tag (`Resume - Alex Bacallao`) renders correctly via the bare-title-plus-template approach (`title: 'Resume'` in `app/resume/page.tsx` metadata; layout's `%s - Alex Bacallao` template appends the suffix once); matches spec § 6.11 exactly (22 chars). Meta description (`Software developer in Ft Worth, Texas. 4 years across embedded systems, healthcare, and industrial automation. Currently Digital Specialist at T&S Machines.`, 157 chars), canonical (`https://alexbacallao.com/resume`), OG title / description / url / type all present and correct. One JSON-LD block (`ProfilePage` with `Person` mainEntity) confirmed via `grep -oE '<script type="application/ld\+json"' /tmp/resume.html | wc -l` (count = 1; Resume has no breadcrumb because the content/profile page pattern doesn't carry one, same as About). `Person` schema fields ship verbatim per spec § 6.11: `name: 'Alejandro Bacallao'`, `alternateName: 'Alex Bacallao'`, `jobTitle: 'Digital Specialist'`, `worksFor` T&S Machines and Tools / Gainesville TX, `alumniOf` UNT, `knowsLanguage` English/Spanish/German with ISO codes, `knowsAbout` 12 entries, `homeLocation` Ft Worth/TX/US, `sameAs` LinkedIn + GitHub, `award` 3 entries (`["1st place, Migros challenge, HackZurich 2022", "1st place, UNT Robotics Competition 2020", "3rd place, UNT Robotics Competition 2019"]` verbatim per spec). Email and phone intentionally omitted.

**Title-template regression check (all nine pages).** All nine prerendered routes still render their spec titles after the step 13 commit:
- `/` → `Alex Bacallao - Software Developer`
- `/about` → `About - Alex Bacallao`
- `/projects` → `Projects - Alex Bacallao`
- `/projects/nasa-circadian-lighting` → `NASA Circadian Lighting - Alex Bacallao`
- `/projects/ts-machines` → `CNC software at T&S Machines - Alex Bacallao`
- `/projects/hackzurich-migros` → `Supply chain rerouting at HackZurich - Alex Bacallao`
- `/projects/nicular` → `Patient messaging at Nicular - Alex Bacallao`
- `/projects/sofabot` → `SofaBot - Alex Bacallao`
- `/resume` → `Resume - Alex Bacallao` (NEW)

**Cross-page sync.** This page does not render project cards. `app/_data/projects.ts` is unchanged by this step (verified pre-build per the plan: Nicular's `href: '/projects/nicular'` at line 83 stays as-is so the /projects index card keeps linking; the Resume page deliberately omits the link from the Projects closing prose per spec § 6.8). Outbound `<Link>`s on the page resolve to live routes: § 03 Role 1 case-study link to `/projects/ts-machines` (live since step 8), § 04 Education embedded link to `/projects/nasa-circadian-lighting` (live since step 7), § 07 Projects bullets 1-3 to `/projects/ts-machines`, `/projects/nasa-circadian-lighting`, `/projects/hackzurich-migros` (all live). The Education paragraph's `→` arrow on the NASA link ships as part of the visible link text, not as a CSS pseudo-element, per the standing site convention.

**Component placement decisions.** `MonoTable` co-located at `app/resume/_components/MonoTable.tsx` rather than `app/_components/`. Four in-code callers on /resume justify extraction; sitewide promotion needs a second route actually importing the component, which doesn't exist today. Step 14 (Contact) is spec'd to use the same pattern; if /contact actually imports it during step 14, promote then with a `git mv` + two import-path updates. The cost of designing for an imagined contact-table shape is larger than the move-cost when the real shape arrives. Same logic placed `RoleEntry` at `app/resume/_components/RoleEntry.tsx`. Memory `feedback_abstraction_threshold.md` updated to record the "in-code callers" refinement during step 13 plan review.

**`RoleBulletList.description` widening.** Type widened from `string` to `React.ReactNode` to enable shipping the spec § 6.8 HackZurich projects bullet's inline bold-italic on `1st place, Migros challenge.` verbatim. Backwards-compatible with all 4 existing string callers (NASA / T&S / HackZurich / Nicular My Role bullets). Not a deviation - normal implementation that enables shipping the locked spec verbatim. Memory `feedback_deviations_log_scope.md` added during step 13 plan review to codify the "what NOT to log in deviations.md" rule (don't log kickoff miscounts, backwards-compatible widenings, or speculative-branch alignments).

**Skills-table source-casing rule.** All cell strings are natural source casing in code (`'Python'`, `'Docker'`, `'Native'`, `'A2 / B1'`, `'Git · GitHub · GitLab'`); the `MonoTable` component applies `text-transform: uppercase` via the cell `uppercase` Tailwind utility, producing the visual ALL-CAPS at JetBrains Mono 12px, +0.1em tracking. Same source-casing rule as everywhere else on the site (memory `feedback_source_casing.md`). Empty cells render as a literal blank string, not `—` or `N/A`. Spoken languages is the only table with a visible `<thead>` (column meanings asymmetric: `Language` / `Level` per SITE_SPEC § 1.10.2); the other three tables ship a sr-only `<thead>` per docs/CLAUDE.md a11y rule.

**Skills-table item count notes.** Spec § 6.6 ships **13 items** in Programming languages (counting Assembly as the 13th, in row 7's left cell with an empty right cell) and **11 cells** in Tools & platforms (with `Git · GitHub · GitLab` as one grouped chip per the spec table). The step-13 kickoff prompt mentioned "12 items" for Programming languages and "10 items" for Tools & platforms — those were kickoff miscounts, not spec drift. Honored the spec verbatim. NO deviation entries for these counts; per the deviations-log scope rule, kickoff miscounts don't divergence-log.

**Design review.** Fraunces / Inter / JetBrains Mono used per spec. Color tokens used throughout (`text-text-primary`, `text-text-secondary`, `text-text-muted`, `text-accent`, `bg-border-rule`, `border-border-hairline`). No hardcoded hex values. § 01 PDF download line ships as a single `<a href="/resume.pdf" download>` with the arrow `↓` as part of the visible link text and the `Updated Apr 2026` date stamp inside the same anchor in `mono-label` muted style (CSS uppercases to `UPDATED APR 2026`); the link `download` attribute is browser-native, no JS interception. Section eyebrow source casing follows the standing site-wide rule (natural casing, hyphen-minus): `§ 01 - Resume`, `§ 02 - Summary`, `§ 03 - Work`, `§ 04 - Education`, `§ 05 - Skills & languages`, `§ 06 - Awards`, `§ 07 - Projects`. None of the seven eyebrows uses middot; all use hyphen-minus per the site-wide deviation. Body sections cap at `max-w-[65ch]` per spec; dek caps at `max-w-[60ch]`. Role entry headers stack on mobile (<768px) and right-align on desktop (>=768px) per spec § 6.4.

**Code quality.** No unused imports (verified by `npm run build` strict TS pass on Next.js 16.2.4 + Turbopack; zero warnings after the readonly→mutable RoleBulletItem array fix during build). No `any`. No console.log. No inline styles (Tailwind utilities throughout). `prefers-reduced-motion` respected by `RevealOnScroll`'s runtime `matchMedia` check. Page returns a fragment per the locked Site-Wide Deviation (root layout owns `<main>`); no double-wrap. Three new files created (`app/resume/page.tsx`, `app/resume/_components/RoleEntry.tsx`, `app/resume/_components/MonoTable.tsx`); one existing file edited (`app/_components/RoleBulletList.tsx` description-type widening).

**Responsive.** H1 36px / 56px, H2 26px / 32px (from `globals.css` element defaults). RoleEntry header rows stack below 768px (flex-col → flex-row at md breakpoint). Skills wall `MonoTable` rows are `w-1/2` per cell, so on narrow viewports the cells share half-width; if the rendered density looks too tight on mobile during the user's browser pass, the fix is to add a min-cell-width or bump the gap. Section dividers render at 80px desktop / 60px mobile per `SectionDivider`.

**Build and performance.** `npm run build` passes with zero warnings; the new route prerenders as static (`○ /resume` in build output). Lighthouse run is deferred to step 18 per the build order; targets remain LCP < 1.5s, CLS 0, INP < 200ms. The page has zero images; LCP candidate is the H1 text node. CLS should stay 0 with margin to spare since the prose-and-table layout has no late-loading layout-shifters.

**Hook fired path.** Post-commit hook should fire Level 3 (new page) given the d39d906 chained-command fix; `app/resume/page.tsx` matches the new-page regex. The Level 3 review work is preempted into the same commit, matching the steps 7-12 precedent. The hook's checklist items not addressed in-commit (Lighthouse run; full browser scroll on the user-flagged visual-rhythm items: role entry header alignment + skills wall density + bold-italic on the HackZurich projects bullet) are deferred to step 18 and to the user's manual scroll pass per CLAUDE.md and the standing user directive on this commit (do NOT push - surface the result for visual review first).

**Open items / decisions to record (no fixes):**
- **Resume PDF still pending.** `/public/resume.pdf` is hand-maintained by Alex; the HTML page links to `/resume.pdf` whether or not the file exists. Pending Items entry remains active. The HTML's `Updated Apr 2026` date stamp must be kept in sync with the PDF's footer/header date stamp per spec § 6.15; drift between the two is the maintenance hazard for this two-format pattern.
- **Resume + About narrative consistency.** Career arc, throughline idea, and language capabilities appear on both pages. Each uses its own genre voice (Resume: dry summary; About: narrative prose). The throughline phrase is verbatim-locked on Resume only; About's throughline is its own phrase. The SITE_SPEC § 1.11 drift is logged from step 12; no new entry needed.
- **SofaBot outreach drop closed.** The `pass2-resume.md` § 07 source markdown ships closing prose without an outreach mention (verbatim per spec § 6.8); the standing Pending Items line about applying the SofaBot outreach drop to the resume is now resolved by step 13 verbatim adherence. Pending Items entry struck through.
- **Don't push after this commit.** User directive: surface the result for visual review first. Resume is the heaviest content page on the site; the visual pass has to verify role entry header alignment (dates right-aligned on desktop, stacked on mobile), skills wall density (4 mono tables stacked with 32px gaps + Inter 500 14px subheads above each, only Spoken languages with a visible header row), and the bold-italic on the HackZurich projects bullet (`1st place, Migros challenge.`). Push only after explicit user approval.
- Dev server check ran against port 3000 (existing background `next dev` instance from a prior session, PID 28292). HTTP 200 on `/resume`, on the eight other prerendered routes (homepage, /projects, /about, all five project pages). A direct browser scroll across all four breakpoints is still owed before crossing the page off the user-memory "browser-check before declaring UI done" rule.
